var MerchantBusinesses = (function() {
    
    var view = function(vnode) {
        
        
        return m(".card", 
                  [m(".card-header", "Businesses"),
                   m(".card-body", m("table.table")),
                   ]);
    }
    
})();